<nav class="bg-gradient-to-r from-white to-gray-200 backdrop-blur-lg p-2 mb-6">
  <div class="max-w-full mx-auto grid grid-cols-12">
    <div class="md:col-span-6 col-span-12 p-1">
      <%= link_to dashboard_path do %>
        <h1 
          class="
            uppercase text-center ml-5
            lg:text-start md:text-start 
            font-bold text-orange-500 text-3xl my-auto
            transition ease-out-in 
            hover:-translate-y-0 hover:scale-110
            duration-300
          "
        >
          <%= "#{current_user.first_name} #{current_user.last_name}" %>
        </h1>
      <% end %>
    </div>
    <div class="col-span-12 md:col-span-6 justify-center md:justify-end p-1 px-5">
      <div class="col-span-3 py-1 font-bold text-center">
        Your balance is: 
        <button 
          class="m-0
            bg-orange-500
            text-white px-2 py-1 my-auto 
            rounded transition ease-in-out 
            hover:-translate-y-0 hover:scale-95
            duration-300
          "
        >
          <%= number_to_currency(current_user.balance, precision: 2) %>
        </button>
      </div>
    </div>
  </div>
</nav>